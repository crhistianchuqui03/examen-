package com.example.mspagoclase.repository;

import com.example.mspagoclase.entity.Pagoclase;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PagoclaseRepository extends JpaRepository <Pagoclase, Integer> {
}
